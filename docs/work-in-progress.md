# Work in Progress

Progress tracker for building the ingestion pipeline. See [data-ingestion.md](data-ingestion.md) for design.

## Steps

1. **Minimal project skeleton** — `pyproject.toml` + deps, `src/` layout (uv). ✅ Done
2. **Container** — `infra/ingestion/Dockerfile` + `docker-compose.yml`, runs `dagster dev`. Single execution path. Persistent `DAGSTER_HOME` on a named volume. ✅ Done (hello-world asset materializes)
3. **Storage/manifest contract + local-FS store** — `manifest.py` (`ManifestRecord`) + `storage.py` (`RawStore`), JSONL manifest, content-addressed raw files. ✅ Done (idempotency verified via smoke test)
4. **EDGAR client → `raw_filings` Dagster asset** — `edgar.py` (throttled httpx client) + `raw_filings` asset. ✅ Done (fetched 3 Apple 10-Ks, ~1.5 MB each, into raw store + manifest)
5. **VCR.py test** — `tests/test_edgar.py`, one cassette per test (`record_mode="once"`, `filter_headers` strips User-Agent). Covers `recent_filings` + `fetch_document`, replays offline. ✅ Done (both pass, no network)
6. **Retries with exponential backoff** — `_get` in `edgar.py` uses tenacity: retry on 429/5xx + transport errors, exp backoff, 5 attempts, reraise on give-up. Transport seam in `__init__` for testing. `tests/test_edgar_retry.py` (retry-then-succeed, give-up) uses `httpx.MockTransport`. ✅ Done (4 tests pass). No `Retry-After` handling yet — deferred.
7. **Contract acquisition & storyline** — Selected and downloaded 3 Apple material contracts into `data/raw/seed_contracts/AAPL/` and created `docs/contracts-storyline.md` connecting them to Apple 10-K sections for cross-document Q&A (*Governance, Equity Plans & Supply Chain Risk*). ✅ Done
8. **Parsing** — Extracted raw HTML filings into clean Markdown using BeautifulSoup and markdownify, retaining tables per design, with idempotency handled in `ParsedStore`. Added to Dagster asset DAG. ✅ Done
9. **Chunking** — `chunker.py` `MarkdownChunker`: custom code separates tables (kept whole) from prose, then LangChain `MarkdownHeaderTextSplitter` + `RecursiveCharacterTextSplitter` split prose by header then size (~8000 chars / 800 overlap ≈ 2000/200 tokens). `ChunkStore` in `storage.py` writes one JSON file per doc (`{natural_id, chunks}`) + `chunk_manifest.jsonl`, idempotent by natural_id. `chunked_filings` asset (deps on `parsed_filings`). DAG: raw → parsed → chunked. See [chunking-strategy.md](chunking-strategy.md). ✅ Done
10. **Embedding & vector index** — `embedder.py` `Embedder`: thin LiteLLM `embedding()` wrapper (embed-only, dumb client), provider-agnostic via `EMBEDDING_MODEL` model string + generic api-base env; preserves input order by sorting `response.data` on index. `vector_store.py` `VectorStore`: Chroma `PersistentClient(data/chroma/)`, collection `chunks`, `has_natural_id` idempotency seam, `add_chunks` (id = `natural_id:chunk_index`, flattens `headers` to scalar metadata, records embedding-model name per chunk). `embedded_chunks` asset (deps on `chunked_filings`) walks `chunk_manifest.jsonl`, batch-embeds each un-embedded doc, writes vectors + metadata. Ollama runs as a compose service (`nomic-embed-text`, 768-dim, auto-pulled; Zscaler-proxy CA handled via `Dockerfile.ollama`). DAG: raw → parsed → chunked → embedded. Tests: `test_embedder.py` (unit, mocked provider, order preservation), `test_vector_store.py` (hermetic integration, real ephemeral Chroma). See [embedding-strategy.md](embedding-strategy.md). ✅ Done (415 vectors materialized)

11. **Query rewrite (online path, stage 1)** — `query/rewriter.py` `QueryRewriter`: thin LiteLLM `completion()` wrapper (dumb client), reads `QUERY_REWRITE_MODEL` + `LLM_API_BASE` from env. Takes a question (+ optional session-context string) and returns a validated Pydantic `QueryRewrite` = `{query_type: "compare"|"other", rewritten_query, filters}`; `filters` = company/doc_type/period (optional, list-capable) + version (default `current`). Structured output via `response_format=QueryRewrite`. System prompt enforces explicit-only filter extraction (no inference) and a narrow `compare` definition. Model: `qwen2.5:3b-instruct` via Ollama. New infra: shared `llm-net` network, `infra/ollama/` (owns Ollama + model pulls, both `nomic-embed-text` and the chat model) and `infra/serving/` (online-path stack, own Dockerfile). `infra/ingestion/` left unchanged for now. Test: `test_query_rewriter.py` (VCR cassettes, real LiteLLM→Ollama call recorded once, replays offline). See [query-rewrite-strategy.md](query-rewrite-strategy.md). ✅ Done (single-fact + compare cases verified)

12. **Prompt versioning (Phoenix)** — Arize Phoenix as a single-container prompt registry + observability stack (`infra/observability/`, image `arizephoenix/phoenix:20.3.0`, SQLite backend, on `llm-net`). Prompts are declared in `seed/prompts/manifest.toml` (logical key → name/label/file) with text in `seed/prompts/*.md`. `prompts/seed.py` idempotently registers each entry into Phoenix and tags it (`production`). `prompts/registry.py` owns registry access: `load_manifest()` + `fetch_system_prompt(key)` (fetch the version at the entry's label, fall back to the seed file if Phoenix is unreachable). `QueryRewriter` fetches its prompt at init (cached), with a `system_prompt` injection seam so tests need no live Phoenix. Only `PHOENIX_ENDPOINT` is in `.env`; name/label live in the manifest. Source of truth is Phoenix after seeding; UI edits go live by re-pointing the label. See [observability.md](observability.md). ✅ Done (live fetch + offline tests verified)

13. **Company resolver (dummy) + pipeline composition** — `query/resolver.py` `CompanyResolver`: surface form → canonical id, deterministic and separate from the rewrite LLM. `resolve(list[str])` returns per-input `Resolution{input, outcome (resolved/not_found/ambiguous), canonical?, match_type?, candidates?}` matching the documented contract. Current implementation is a dummy: exact lookup against a hardcoded Apple registry (no alias/fuzzy/ambiguous yet) — placeholder so the call site exists. `query/pipeline.py` `QueryPipeline.run()` composes the online path so far: query rewrite → resolve `filters.company` → `ResolvedQuery{rewrite, companies}`; both stages injectable. Tests: `test_company_resolver.py` (exact/case-insensitive/not_found/order) and `test_pipeline.py` (real `QueryRewriter` + resolver, VCR-recorded LiteLLM→Ollama, wiring + empty-filter guard). See [query-rewrite-strategy.md](query-rewrite-strategy.md). ✅ Done (in-container end-to-end run verified: Apple → AAPL)

14. **Ingestion metadata (filter fields on chunks) + config/registry** — Stamps the four retrieval filter fields (`company`/`doc_type`/`period`/`version`) onto every chunk's vector metadata so the resolver and metadata-filtered retrieval have real fields. What to ingest is now **config**, not hardcoded: `config/companies.toml` (`tickers`, `forms`) drives `raw_filings`, replacing the hardcoded CIK/form. `company_registry.py` `CompanyRegistry` owns the cik↔ticker↔name lookup, built from SEC's `company_tickers.json` — fetched once per run by the (dumb) `EdgarClient.fetch_company_tickers()` and written to a **stable path** `data/reference/company_tickers.json` (not content-addressed like `RawStore`; it's re-fetchable reference data, so latest-wins overwrite, with a sha256 change-check that skips the write and logs when unchanged). `raw_filings` resolves each ticker → CIK/name and stamps `ticker`/`company_name`/`report_date` into `source_metadata`; these flow through the parsed/chunk manifests. `metadata.py` `chunk_filter_fields(source_metadata)` is a pure map → `{company: ticker, doc_type: form, period: report_date[:4], version: "current"}` (period from the fiscal-period end, not filing date; `version` static until a re-filed same-period doc exists; missing ticker omits `company`). Derivation happens once, at **chunk time**: `chunked_filings` calls `chunk_filter_fields` and `ChunkStore.put` writes a document-level `metadata` block into the chunk JSON (`{natural_id, metadata, chunks}`), so the chunk file is self-describing (chunk-level attrs — headers/is_table/chunk_index — stay per chunk). `embedded_chunks` then just reads `payload["metadata"]` and `vector_store.add_chunks(filter_metadata=…)` merges it onto every vector — single derivation point, chunk file is the source of truth. Tests: `test_company_registry.py` (VCR — real SEC `company_tickers.json`, incl. unchanged-skip via replay + update via a pre-seeded stale file), `test_metadata.py`, `test_edgar.py` (`report_date` + `fetch_company_tickers`, VCR), `test_embedder.py` (VCR — real Ollama embedding), `test_vector_store.py` (fields stamped), `test_storage.py` (`ChunkStore` writes the metadata block) — 27 pass. Cassettes recorded in-container (SEC/Ollama reachable there), replay offline. ✅ Done (re-indexed: 415 vectors, all stamped `company=AAPL`, `doc_type=10-K`, `period` 2023/2024/2025, `version=current`, 0 missing company).

15. **Retrieval as an MCP server (`search_filings`)** — First online-path tool exposed over MCP. New `retrieval/` subpackage: `search.py` holds the logic (`FilingSearch` wraps `Embedder` + `VectorStore`; embeds the query with the *same* model as the corpus, builds a Chroma `where` from the four filter fields — list fields → `$in`, scalar `version` → equality, multiple `$and`-ed, empty → `None` since Chroma rejects `{}` — and shapes results into `SearchResult{text, distance, citation}` where `Citation` carries the stamped provenance + folded-back header levels). `server.py` is a thin MCP adapter (mcp 2.x SDK, `MCPServer`, one `@mcp.tool()` `search_filings(query, company?, doc_type?, period?, version, top_k)`) run over **streamable-http** so any host can call it. The tool is deliberately dumb: confirmed/canonical filters in, chunks out — rewrite/resolve/gate stay upstream. Added `VectorStore.query()` (Chroma read stays in the store) + a `CHROMA_PATH` env default so the server constructs one with no args. New dep `mcp>=2.1.0`. New infra stack `infra/retrieval/` on `llm-net` (own Dockerfile, publishes 8001, sets `CHROMA_PATH=/app/data/chroma`). Tests: `test_filing_search.py` — real ephemeral Chroma seeded with *real* recorded nomic-embed-text vectors (both corpus + query embeddings via VCR, `match_on` body; no fabricated 768-d fixtures) asserting company-filter restriction + full citation, plus `_build_where` unit cases. 33 pass. Verified end-to-end in-container: real-index search (period filter isolates the 2024 10-K; unfiltered spans 2023/24/25) and a full MCP client handshake (`list_tools` → `call_tool`) over http returning cited 2024 Apple passages. See [retrieval-and-serving.md](retrieval-and-serving.md). ✅ Done

16. **Orchestrator (MCP client) + HITL confirmation gate** — New `serving/` subpackage composing the two-call online flow. `serving/search_client.py` `SearchClient`: the outbound MCP edge — an async client that opens a fresh streamable-http session per call to `SEARCH_MCP_URL`, calls the `search_filings` tool (`{query, top_k, **filters.model_dump()}`), and parses `CallToolResult.structured_content` (`{"result": [...]}`) back into `retrieval.SearchResult`s. `serving/orchestrator.py` `Orchestrator`: `interpret(question)` runs `QueryPipeline` (rewrite→resolve), then **substitutes the resolved canonical ticker into the proposed filters** (the corpus is stamped `company=AAPL`, so the surface form "Apple" must become "AAPL" or retrieval finds nothing — the bug the live smoke caught; unresolved inputs keep the surface form and force confirmation), and computes a `needs_confirmation` flag; `answer(question, confirmed_filters)` (async) retrieves via the MCP tool on the **confirmed** filters — no re-run of rewrite/resolve, which is the whole point of the gate. `_needs_confirmation` policy: surface filters always, force an explicit confirm on weak signals (any `not_found`/`ambiguous` resolution, or a multi-company `compare`), returning per-reason strings. `SearchClient` is built lazily (in `answer`) so `interpret` needs no `SEARCH_MCP_URL`. New env `SEARCH_MCP_URL`. Tests: `test_orchestrator.py` — pure gate-policy cases, `interpret` ticker-substitution/surface-form-kept/none-stays-none, and `answer` wiring via a fake async client (`asyncio.run`, no plugin). 42 pass. Verified live end-to-end: `interpret` proposed `company=['AAPL']`, `answer` retrieved two 2024 10-K passages through the real MCP server. No FastAPI yet (step 3). ✅ Done

17. **FastAPI entry point (`serving/api.py`)** — Thin online-path API over the `Orchestrator`: `GET /health`, `POST /interpret` (body `{question, session_context?}` → `Interpretation`), `POST /answer` (body `{question, filters, top_k?}` → `Answer`, async). Orchestrator is a lazy singleton behind a `get_orchestrator` FastAPI dependency, so importing the module has no side effects and tests override it. Serving Dockerfile CMD now targets `financial_doc_ai.serving.api:app`; serving compose sets `SEARCH_MCP_URL=http://retrieval-app-1:8001/mcp`. New deps `fastapi`, `uvicorn`. Tests: `test_api.py` (TestClient + dependency-override fake orchestrator — health, interpret gate/filters shape, answer passes posted filters+top_k through). **45 tests pass in-container.** ✅ Live full-stack HTTP smoke passed (2026-08-28, post-restart): `/health` ok; `/interpret` "Apple 2024 10-K" → `company:[AAPL] doc_type:[10-K] period:[2024]`, `needs_confirmation:false`; `/answer` (HTTP → orchestrator → MCP → retrieval) returned 3 correctly-filtered AAPL/2024/10-K chunks with citations. ✅

18. **Async online path** — Made the whole online path `async` end to end so the FastAPI worker is free to serve other requests during the (slow, seconds-long) LLM wait. `QueryRewriter.rewrite` now `await`s `litellm.acompletion` (was blocking `completion`); `QueryPipeline.run`, `Orchestrator.interpret`, and the `POST /interpret` route are `async` and `await` down the chain. **Resolver stays sync** — it's a fast in-memory dict lookup; called directly (no `await`) inside the async pipeline. Rationale: FastAPI runs a *sync* handler in a capped (~40) threadpool, so many concurrent seconds-long `interpret` calls would exhaust it and queue; async parks thousands of waits cheaply on the event loop. Tests switched to `asyncio.run(...)` (repo convention, no pytest-asyncio); stub/fake `run`/`interpret` made `async`. VCR matched the existing cassettes through the async path — no re-recording needed. 45 tests pass; live async `/interpret`+`/answer` re-smoked. See [async-vs-sync.md](async-vs-sync.md). ✅

19. **Strip running headers/footers in the parser** — The `/answer` smoke surfaced junk chunks that were just page footers (`Apple Inc. | 2024 Form 10-K | 57`): the parser kept the running footer on every page, and the table/prose splitter isolated each one-line footer between tables into its own chunk. Fix at the source: `FilingParser._strip_repeated_lines` counts each line's *normalized* form (digits→`#`, whitespace squeezed) and drops originals whose form recurs ≥ `footer_repeat_threshold` (default 5). Frequency is the signal — running furniture repeats ~50×, a footnote appears once, so footnotes and numbers in kept lines are untouched (normalization is a throwaway comparison key, never written). Blank + table lines (`|`…) are never counted or dropped. This is format-agnostic cleanup that runs after HTML extraction; when PDF upload lands, the same step follows the PDF extractor (revisit `unstructured` as a unified extractor then). Tests: `test_parser.py` (footer stripped / below-threshold kept / table rows never dropped). **Re-ran ingestion** (cleared parsed+chunks+chroma, kept raw; materialized parsed→chunked→embedded): 399 chunks (was 415), **0 footer-only chunks**. Live `/answer` now returns the real *Item 1A Risk Factors* prose instead of a page footer. 48 tests pass. ✅

## What the downloaded files are

### 1. 10-K Filings
Each raw file is a **10-K** — a company's mandatory annual report to the SEC (once a year, ~hundreds of pages). We fetched Apple's for FY2025/2024/2023.

Standard 10-K sections (same structure across all companies):
- **Business** — what the company does, products/services.
- **Risk Factors** — everything that could go wrong (competition, supply chain, legal, macro).
- **MD&A** — management explaining in words how the year went and why.
- **Financial Statements** — the core numbers: net sales/revenue, profit, assets, debt, cash.
- **Notes** — fine-print on how the numbers were derived.

Format: **HTML with inline XBRL**. Key numbers are machine-tagged (e.g. `us-gaap:RevenueFromContract...`, `ix:nonNumeric` tags). This gives two uses of the same file:
- **Human text** (business, risk factors) → what RAG retrieves and cites.
- **Tagged XBRL numbers** → exact ground-truth figures to verify answers against (ties to the "reconcile against ground truth" point in [data-ingestion.md](data-ingestion.md)).

### 2. Material Contracts (Exhibit 10s)
Downloaded 3 PDFs into `data/raw/seed_contracts/AAPL/` to ground high-level 10-K disclosures in legal text:
- `320193_000114036126006577_ef20060722_ex10-1.pdf` — **Director Stock Plan (`EX-10.1`)** (Governance / Compensation caps).
- `320193_000114036126006577_ef20065677_ex10-2.pdf` — **Form of RSU Award Agreement (`EX-10.2`)** (Equity vesting / Forfeiture).
- `320193_000110465905058421_a05-20674_1ex10dbd19.pdf` — **Component Purchase Agreement (`EX-10.B.19`)** (Supply chain risk / Supplier remedies).

See [contracts-storyline.md](contracts-storyline.md) for full context, 10-K section pairings, and showcase analyst queries.

## Next session — pick up here

- **Ingestion metadata + config/registry are done** (step 14), and the corpus was re-indexed (415 vectors, all filter fields verified). To rebuild from scratch again: `docker compose -f infra/ingestion/docker-compose.yml up -d --build` then `... exec app uv run dagster asset materialize --select '*' -m financial_doc_ai.ingestion.definitions` (embed stage ~10 min on CPU). The package was also **restructured by subsystem** (`ingestion/`, `query/`, `storage/`, `prompts/` + top-level `company_registry.py`) — see CLAUDE.md "Stack & layout".
- **Decision (design doc: [retrieval-and-serving.md](retrieval-and-serving.md)):** next build is **retrieval implemented as an MCP server** (`search_filings(query, filters)` wrapping `VectorStore` + query embedding), driven by a minimal **orchestrator = MCP client** that puts a **human confirmation gate** in front of the retrieve call (the analyst verifies/corrects the extracted filters before retrieval — guards the mis-extraction risk). HITL kept simple/stateless: two calls, `interpret(question)` → proposed filters, then `answer(question, confirmed_filters)`. A thin FastAPI (`/interpret`, `/answer`) exercises it; no web UI yet. Query rewrite + resolver stay internal (not MCP). **Company resolver upgrade is deferred (low priority).**
- Concrete next steps: ✅ (1) `search_filings` MCP server (step 15). ✅ (2) orchestrator/MCP-client with the confirm gate (step 16). ✅ (3) FastAPI entry point (step 17, code done + 45 tests green; live smoke pending — see below).
- **Stack bring-up sequence** (dev == deploy; run after a Docker restart): `docker network create llm-net` (ignore "already exists"); `docker compose -f infra/ollama/docker-compose.yml up -d` + `docker compose -f infra/observability/docker-compose.yml up -d` (wait for `ollama` healthy — models persist in the volume); then `MSYS_NO_PATHCONV=1 docker compose -f infra/retrieval/docker-compose.yml up -d --build` then the same for `infra/serving/`. Smoke: `curl localhost:8000/health`; `/interpret` "Apple 2024 10-K" → `company:["AAPL"]`; POST those filters to `/answer` → cited 2024 10-K chunks.
- **Next: generation + grounding + audit log.** Note mcp 2.x client API: `from mcp.client.streamable_http import streamable_http_client` yields a 2-tuple `(read, write)`; `CallToolResult.structured_content` (snake_case) holds the tool's return (list wrapped as `{"result": [...]}`).
- On Windows/Git Bash, prefix docker commands passing `-e /app/...` with `MSYS_NO_PATHCONV=1` (path-mangling); compose-file env is unaffected.
- To run the retrieval MCP server: `docker compose -f infra/retrieval/docker-compose.yml up -d --build` (needs the `ollama` container up on `llm-net` for query embedding). On Windows/Git Bash, prefix docker commands that pass `-e /app/...` with `MSYS_NO_PATHCONV=1` or the path gets mangled to a Windows path (compose-file env is unaffected).
- Optional observability follow-on: **Phoenix tracing** (OpenTelemetry spans over the LiteLLM→Ollama calls) — Phoenix is already running as the prompt registry.
- Prompt tuning: `query/rewriter.py` system prompt is tuned against a few example questions; revisit with a proper labelled set when building filter-extraction eval.
- Deferred: `Retry-After` header handling on 429s; session-context representation (see strategy doc open items); versioning/supersession of filings (mark older same-period filings `superseded` once a 10-K/A is in the corpus).

## Step 1 — Project skeleton

- Tool: **uv** (`uv init --package`), `src/` layout → importable package `src/financial_doc_ai`.
- Python pinned to **3.12** (`.python-version`); `requires-python = ">=3.12"`.
- Deps: `dagster`, `dagster-webserver`, `httpx` (runtime); `pytest`, `vcrpy`, `ruff` (dev).
- Lockfile `uv.lock` committed for reproducible installs.

## Step 2 — Container (single execution path)

Dev and deploy share one image, so there's no separate host execution path (deliberate — dev == deploy).

- `infra/ingestion/Dockerfile`:
  - Base `python:3.12-slim`; uv binary copied from `docker.io/astral/uv:latest`.
  - Deps installed before app code (`COPY pyproject.toml uv.lock` → `uv sync --frozen --no-install-project`) for layer caching; then `COPY . .` → `uv sync --frozen`.
  - `--frozen` → installs exactly from `uv.lock`.
  - `CMD` runs `dagster dev -h 0.0.0.0 -p 3000`.
- `infra/ingestion/docker-compose.yml`:
  - `build.context: ../..` (repo root, so COPY sees `pyproject`/`src`), `dockerfile:` points back to the Dockerfile.
  - Bind mount `../..:/app` → live code edits without rebuild.
  - Anonymous volume `/app/.venv` → keeps container's Linux venv, unshadowed by host mount.
  - Named volume `dagster_home:/opt/dagster_home` + `DAGSTER_HOME` env → persistent Dagster storage (run/asset history survives restarts). Named volume avoids Windows→Linux bind-mount issues for SQLite.
- Infra layout: per-stack under `infra/<stack>/` (here `ingestion/`); umbrella for future compose stacks + cloud IaC.
- Dagster loads code via `[tool.dagster] module_name = "financial_doc_ai.ingestion.definitions"` in `pyproject.toml`.
- Run: `docker compose -f infra/ingestion/docker-compose.yml up --build` → UI at `http://localhost:3000`.
- Note: `dagster dev` is a dev server; production splits into `dagster-webserver` + `dagster-daemon` (revisit when adding schedules).

## Manifest contract (step 3)

Acquisition asset outputs per filing: (a) raw bytes at a content-addressed path, (b) a manifest record.

Manifest record fields:
- `natural_id` — source's stable ID (EDGAR: accession number)
- `content_hash` — SHA-256 of raw bytes (idempotency + integrity)
- `source` — `edgar` / `cuad`
- `storage_path` — where raw bytes landed
- `fetched_at` — retrieval timestamp
- `source_metadata` — source-specific dict (EDGAR: CIK, form type, filing date, company)

Principles:
- **Idempotency** — hash bytes; skip if hash already in manifest.
- **Supersession by natural ID, never overwrite** — new version = new record; old records kept as audit evidence.

Manifest storage: **JSONL for now** (simple, no DB); migrate to SQLite/cloud DB later.
