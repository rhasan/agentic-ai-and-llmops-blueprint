# Virtual Environment
.venv/
venv/
ENV/

# Python Cache
__pycache__/
*.py[cod]

# Secrets & Environment Variables
.env
.env.*
!.env.example
# Project specific secrets
docs/refs
# CA cert (machine/network-specific; run infra/<stack>/setup-cert.ps1)
infra/*/certs/*
!infra/*/certs/.gitkeep

# OS / Editor files
.DS_Store
Thumbs.db
.vscode/
.idea/
.vs

# pytest
.pytest_cache

# Claude related
# .claude
# CLAUDE.md
.claude/settings.local.json

# data (raw filings + manifest — regenerated by the ingestion pipeline)
data/

# Agents
.agents/

# other dot files
.ruff_cache

# specs handed to other models (working scratch, not part of the repo)
docs/specs/

# throwaway scratch (harnesses, one-off probes) — never pushed
scratch/
